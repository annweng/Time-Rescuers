#make a class
class Question:
     def __init__(self, prompt, answer):
     # define the attribute 
          self.prompt = prompt
     # take the value and assign to the variable
          self.answer = answer
          
